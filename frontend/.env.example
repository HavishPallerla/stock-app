# Anon key only — safe to expose in the frontend bundle. Used for Supabase
# Auth session handling and the dashboard's Realtime subscription.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

VITE_API_BASE_URL=http://localhost:4000
